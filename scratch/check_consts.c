#include "tokenizer.h"
#include <stdio.h>
int main() {
    printf("BASE_SYMBOL_OFFSET: %d\n", BASE_SYMBOL_OFFSET);
    printf("MAX_RULES: %d\n", MAX_RULES);
    printf("TOKEN_ID_OFFSET: %d\n", TOKEN_ID_OFFSET);
    printf("TOKEN_ID_REMAP_SHIFT: %d\n", TOKEN_ID_REMAP_SHIFT);
    return 0;
}
